class Onlineshop:
  def __init__(self, name ,url):
    self.name = str(name)
    self.url = str(url)
    self.productss = {}
"""  
  def addingItemsToCart(self, customer, product, quantity):
    self.customer(Customer)
    

class Product:
  def __init__(self, name_product, description, price):
    self.name_product = str(name_product)
    self.description = str(description)
    self.price = float(price)
    self.online_shop(Onlineshop)

class Customer:
  def __init__(self, name_customer, email, address):
    self.name_customer = str(name_customer)
    self.email = str(email)
    self.address = str(address)
    self.cart = {}
    self.past_orders = {}
"""
Store = Onlineshop()
Store = "Gaming"